<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yemekdefteri">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        android:screenOrientation="portrait"
        <activity android:name=".mainpage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".yeniYemekActivity" />
        <activity android:name=".yemekListePage"/>
        <activity android:name=".yemekSilPage" />
        <activity android:name=".yemekInceleActivity"/>
        <activity android:name=".yemekDuzenlePage"/>
        <activity android:name=".guncelle"/>
    </application>

</manifest>